{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --chdir src --bind 0.0.0.0:$PORT app:create_app()",
    "healthcheckPath": "/health"
  }
}